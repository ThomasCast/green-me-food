<div class="container">
  <h1>Categories</h1>
  <div class="card-category">
    <% @categories.each do |category| %>
      <%= link_to category.name, category_path(category) %>
      <%= image_tag image_url(category.photo_url), width: 150 %>
    <% end %>
  </div>
  <hr>
</div>

